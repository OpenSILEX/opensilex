let PROV_URI = "http://www.w3.org/ns/prov";
let PROV_SHORT_URI = "prov";

let PROV = {
    URI: PROV_URI,
    SHORT_URI: PROV_SHORT_URI,
    ACTIVITY_TYPE_URI: PROV_URI + "#activity",
    ACTIVITY_TYPE_SHORT_URI: PROV_SHORT_URI + ":activity"
};

export default PROV;