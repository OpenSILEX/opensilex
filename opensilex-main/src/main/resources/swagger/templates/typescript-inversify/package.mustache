{
  "name": "{{{npmName}}}",
  "version": "{{{npmVersion}}}",
  "description": "swagger client for {{{npmName}}}",
  "author": "Swagger Codegen Contributors",
  "keywords": [
    "swagger-client"
  ],
  "license": "Unlicense",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --outDir dist/",
    "postinstall": "npm run build",
    "check:outdated": "npm outdated || cd .",
    "check:security": "npm audit || cd .",
    "check:browserlist": "npm add caniuse-lite && npm remove caniuse-lite",
    "build:types": "echo 'export interface ModelObject {};' > ./model/modelObject.ts && tsc --declaration --emitDeclarationOnly --outDir ../../types --skipLibCheck"
  },
  "dependencies": {
    "inversify": "^4.3.0",
    "node-fetch": "^2.6.0",
    "whatwg-fetch": "~2.0.1",
    "reflect-metadata": "0.1.8"
  },
  "devDependencies": {
    "@types/node": "20"
  }{{#npmRepository}},{{/npmRepository}}
{{#npmRepository}}
  "publishConfig": {
    "registry": "{{{npmRepository}}}"
  }
{{/npmRepository}}
}
