<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.0.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.0.0 http://maven.apache.org/xsd/assembly-2.0.0.xsd">
    <id>release</id>
    <formats>
        <format>zip</format>
    </formats>
    <includeBaseDirectory>false</includeBaseDirectory>
    <dependencySets>
        <dependencySet>
            <includes>
                <include>${project.groupId}:*:jar</include>
            </includes>
            <excludes>
                <exclude>${project.groupId}:opensilex:*:${project.version}</exclude>
                <exclude>${project.groupId}:opensilex-swagger-codegen-maven-plugin:*:${project.version}</exclude>
            </excludes>
            <outputFileNameMapping>${artifact.artifactId}.${artifact.extension}</outputFileNameMapping>
            <useProjectArtifact>false</useProjectArtifact>
            <outputDirectory>modules</outputDirectory>
            <unpack>false</unpack>
        </dependencySet>
    </dependencySets>    
    <files>
        <file>
            <source>${project.basedir}/../opensilex/target/opensilex-${project.version}-full.jar</source>
            <outputDirectory>.</outputDirectory>
            <destName>opensilex.jar</destName>
        </file>
        <file>
            <source>${project.basedir}/src/main/assembly/logback.xml</source>
            <outputDirectory>.</outputDirectory>
            <destName>logback.xml</destName>
        </file>        
    </files>
</assembly>