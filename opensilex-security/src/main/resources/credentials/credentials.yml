# This file defines credentials not intended to be used in the API
# E.g., menu access credentials to display menu entries

credentialGroups:
  # Credential groups are defined by :
  # - an id
  # - a label key (optional if the group is already defined in an API)
  # - the credentials in this group
  #   A credential is defined by :
  #   - an id
  #   - a label key (not optional this time)
  - id: Organizations
    credentials:
      - id: organization-access
        label: credential.default.access
  - id: Projects
    credentials:
      - id: project-access
        label: credential.default.access
  - id: Experiments
    credentials:
      - id: experiment-access
        label: credential.default.access
  - id: Facilities
    credentials:
      - id: facility-access
        label: credential.default.access
  - id: Devices
    credentials:
      - id: device-access
        label: credential.default.access
  - id: Events
    credentials:
      - id: event-access
        label: credential.default.access
  - id: Variables
    credentials:
      - id: variable-access
        label: credential.default.access
  - id: Germplasm
    credentials:
      - id: germplasm-access
        label: credential.default.access
  - id: Documents
    credentials:
      - id: document-access
        label: credential.default.access
  - id: "Scientific Objects"
    credentials:
      - id: scientific-objects-access
        label: credential.default.access
  - id: Provenances
    credentials:
      - id: provenance-access
        label: credential.default.access
  - id: Data
    credentials:
      - id: data-access
        label: credential.default.access
  - id: Vocabulary
    label: credential-groups.vocabulary
    credentials:
      - id: vocabulary-access
        label: credential.default.access
  - id: Accounts
    credentials:
      - id: account-access
        label: credential.default.access
  - id: Persons
    credentials:
      - id: person-access
        label: credential.default.access
  - id: Groups
    credentials:
      - id: group-access
        label: credential.default.access
  - id: Profiles
    credentials:
      - id: profile-access
        label: credential.default.access
  - id: Packages
    label: credential-groups.packages
    credentials:
      - id: package-access
        label: credential.default.access
# This should be moved to the opensilex-dataverse module but that would necessitate to adapt the ExtraCredentialService.
  - id: Dataverse
    label: credential-groups.dataverse
    credentials:
      - id: dataverse-access
        label: credential.default.access